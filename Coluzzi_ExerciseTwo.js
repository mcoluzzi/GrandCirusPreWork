do {
		var play = prompt("Do you want to play? Yes/No ");
		if (play != "No") {
		 	var userInput = prompt("Enter a word!");
		} 
  var userString = userString " " + userInput; 
    
}while(play != "No");

alert(userString);